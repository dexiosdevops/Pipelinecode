pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                echo "Building on master"
                
            }
        }
      stage("Deploymenet") {
            steps {
                echo "Building on master"
                echo " connections check connecting via ssh"
                echo " copy the file to tomcat server"
                echo "Hello"
                
            }
        }

    }
}
